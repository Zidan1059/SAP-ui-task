<mvc:View xmlns:mvc="sap.ui.core.mvc" xmlns:core="sap.ui.core" xmlns="sap.m">
    <FlexBox>
        <VBox class="sapUiSmallMarginEnd">
            <ComboBox placeholder="Product"
                items="{
          
                
            }">
                <core:Item key="{key}" text="{text}" />
            </ComboBox>
        </VBox>

        <Input id="Items" value="" showValueHelp="true" valueHelpOnly="true" valueHelpRequest="handleValueHelp" width="100%" class="sapUiSmallMarginBottom" />
    </FlexBox>

</mvc:View>